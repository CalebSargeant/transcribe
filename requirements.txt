# Dependencies are now declared in pyproject.toml (PEP 621).
# This file is kept only so existing tooling that runs
# `pip install -r requirements.txt` still installs the project and its
# runtime + Google Drive dependencies.
#
# Preferred install:  pip install -e ".[gdrive]"   (or: uv sync)
#
# System dependencies (install via Homebrew):
#   brew install whisper-cpp ffmpeg
.[gdrive]
